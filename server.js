const express=require('express');const cors=require('cors');const bcrypt=require('bcryptjs');const jwt=require('jsonwebtoken');const fs=require('fs');const path=require('path');const {Pool}=require('pg');
const app=express();app.use(cors());app.use(express.json());
const usePg=/^postgres(ql)?:\/\//i.test(process.env.DATABASE_URL||'');const pool=usePg?new Pool({connectionString:process.env.DATABASE_URL,ssl:{rejectUnauthorized:false}}):null;const FILE=path.join(__dirname,'data.json');const JWT_SECRET=process.env.JWT_SECRET||'change-this-in-render';const ADMIN_KEY=process.env.ADMIN_KEY||'set-an-admin-key';const prices={1000:22,2000:40,5000:80,10000:150};const sizes=[1000,2000,5000,10000];
function fileInit(){if(!fs.existsSync(FILE))fs.writeFileSync(FILE,JSON.stringify({users:[],orders:[],accounts:[],trades:[],next:{user:1,order:1,account:1,trade:1}},null,2))}function dbRead(){fileInit();return JSON.parse(fs.readFileSync(FILE,'utf8'))}function dbWrite(d){fs.writeFileSync(FILE,JSON.stringify(d,null,2))}async function init(){if(usePg){await pool.query(`CREATE TABLE IF NOT EXISTS users(id SERIAL PRIMARY KEY,name TEXT NOT NULL,email TEXT UNIQUE NOT NULL,password_hash TEXT NOT NULL,created_at TIMESTAMPTZ DEFAULT now());CREATE TABLE IF NOT EXISTS orders(id SERIAL PRIMARY KEY,user_id INT REFERENCES users(id) ON DELETE CASCADE,mode TEXT NOT NULL,size INT NOT NULL,price NUMERIC(10,2) NOT NULL,payment_ref TEXT,status TEXT NOT NULL DEFAULT 'pending',created_at TIMESTAMPTZ DEFAULT now(),approved_at TIMESTAMPTZ);CREATE TABLE IF NOT EXISTS accounts(id SERIAL PRIMARY KEY,user_id INT REFERENCES users(id) ON DELETE CASCADE,account_no TEXT UNIQUE NOT NULL,mode TEXT NOT NULL,size INT NOT NULL,balance NUMERIC(14,2) NOT NULL,equity NUMERIC(14,2) NOT NULL,total_pnl NUMERIC(14,2) NOT NULL DEFAULT 0,daily_pnl NUMERIC(14,2) NOT NULL DEFAULT 0,status TEXT NOT NULL DEFAULT 'active',created_at TIMESTAMPTZ DEFAULT now());CREATE TABLE IF NOT EXISTS trades(id SERIAL PRIMARY KEY,account_id INT REFERENCES accounts(id) ON DELETE CASCADE,direction TEXT NOT NULL,amount NUMERIC(14,2) NOT NULL,entry_price NUMERIC(14,2) NOT NULL,exit_price NUMERIC(14,2) NOT NULL,result TEXT NOT NULL,pnl NUMERIC(14,2) NOT NULL,expiry TEXT NOT NULL,created_at TIMESTAMPTZ DEFAULT now());`)}else fileInit()}
function token(u){return jwt.sign({id:u.id,email:u.email},JWT_SECRET,{expiresIn:'7d'})}async function auth(req,res,next){try{const h=req.headers.authorization||'';req.user=jwt.verify(h.startsWith('Bearer ')?h.slice(7):'',JWT_SECRET);next()}catch(e){res.status(401).json({error:'Authentication required'})}}
app.get('/api/health',(req,res)=>res.json({ok:true,service:'FundedEdge API',storage:usePg?'postgres':'fallback-file',time:new Date().toISOString()}));
app.post('/api/auth/register',async(req,res)=>{try{const {name,email,password}=req.body;if(!name||!email||!password||password.length<8)return res.status(400).json({error:'Name, email and an 8+ character password are required'});const em=email.toLowerCase().trim();const hash=await bcrypt.hash(password,12);if(usePg){const r=await pool.query('INSERT INTO users(name,email,password_hash) VALUES($1,$2,$3) RETURNING id,name,email',[name,em,hash]);return res.json({token:token(r.rows[0]),user:r.rows[0]})}const d=dbRead();if(d.users.some(x=>x.email===em))return res.status(400).json({error:'Email already registered'});const u={id:d.next.user++,name,email:em,password_hash:hash,created_at:new Date().toISOString()};d.users.push(u);dbWrite(d);res.json({token:token(u),user:{id:u.id,name:u.name,email:u.email}})}catch(e){res.status(400).json({error:'Registration failed'})}});
app.post('/api/auth/login',async(req,res)=>{try{const em=String(req.body.email||'').toLowerCase().trim();const pw=req.body.password||'';let u;if(usePg){const r=await pool.query('SELECT * FROM users WHERE email=$1',[em]);u=r.rows[0]}else u=dbRead().users.find(x=>x.email===em);if(!u||!(await bcrypt.compare(pw,u.password_hash)))return res.status(401).json({error:'Invalid email or password'});res.json({token:token(u),user:{id:u.id,name:u.name,email:u.email}})}catch(e){res.status(500).json({error:'Login failed'})}});
app.get('/api/me',auth,async(req,res)=>{if(usePg){const u=(await pool.query('SELECT id,name,email,created_at FROM users WHERE id=$1',[req.user.id])).rows[0];const a=(await pool.query('SELECT * FROM accounts WHERE user_id=$1 ORDER BY id DESC',[req.user.id])).rows;return res.json({user:u,accounts:a})}const d=dbRead();const u=d.users.find(x=>x.id===req.user.id);res.json({user:{id:u.id,name:u.name,email:u.email,created_at:u.created_at},accounts:d.accounts.filter(x=>x.user_id===req.user.id).sort((a,b)=>b.id-a.id)})});
app.post('/api/orders',auth,async(req,res)=>{const {mode,size,price,paymentRef}=req.body;const s=Number(size),p=Number(price);if(!['1 Step','2 Step'].includes(mode)||!sizes.includes(s)||p!==prices[s])return res.status(400).json({error:'Invalid challenge'});if(usePg){const r=await pool.query('INSERT INTO orders(user_id,mode,size,price,payment_ref) VALUES($1,$2,$3,$4,$5) RETURNING *',[req.user.id,mode,s,p,paymentRef||null]);return res.json({order:r.rows[0]})}const d=dbRead();const o={id:d.next.order++,user_id:req.user.id,mode,size:s,price:p,payment_ref:paymentRef||null,status:'pending',created_at:new Date().toISOString(),approved_at:null};d.orders.push(o);dbWrite(d);res.json({order:o})});
app.get('/api/orders',auth,async(req,res)=>{if(usePg){const r=await pool.query('SELECT * FROM orders WHERE user_id=$1 ORDER BY id DESC',[req.user.id]);return res.json({orders:r.rows})}res.json({orders:dbRead().orders.filter(x=>x.user_id===req.user.id).sort((a,b)=>b.id-a.id)})});
app.get('/api/accounts/:id',auth,async(req,res)=>{if(usePg){const r=await pool.query('SELECT * FROM accounts WHERE id=$1 AND user_id=$2',[req.params.id,req.user.id]);if(!r.rows[0])return res.status(404).json({error:'Account not found'});const t=await pool.query('SELECT * FROM trades WHERE account_id=$1 ORDER BY id DESC LIMIT 100',[req.params.id]);return res.json({account:r.rows[0],trades:t.rows})}const d=dbRead(),a=d.accounts.find(x=>x.id===Number(req.params.id)&&x.user_id===req.user.id);if(!a)return res.status(404).json({error:'Account not found'});res.json({account:a,trades:d.trades.filter(x=>x.account_id===a.id).sort((a,b)=>b.id-a.id).slice(0,100)})});
app.post('/api/trades',auth,async(req,res)=>{try{const {accountId,direction,amount,expiry}=req.body;const amt=Number(amount);if(!['CALL','PUT'].includes(direction)||!['60s','5m','15m'].includes(expiry)||!Number.isFinite(amt)||amt<=0)return res.status(400).json({error:'Invalid trade'});let a;if(usePg)a=(await pool.query('SELECT * FROM accounts WHERE id=$1 AND user_id=$2 AND status=$3',[accountId,req.user.id,'active'])).rows[0];else a=dbRead().accounts.find(x=>x.id===Number(accountId)&&x.user_id===req.user.id&&x.status==='active');if(!a)return res.status(404).json({error:'Active account not found'});if(amt>Number(a.balance)*0.2)return res.status(400).json({error:'Trade amount exceeds 20% account limit'});const entry=67482.10+(Math.random()-.5)*100,exit=entry+(Math.random()-.5)*120,win=direction==='CALL'?exit>entry:exit<entry,pnl=win?amt*.8:-amt,result=win?'WIN':'LOSS',created_at=new Date().toISOString();if(usePg){const nr=await pool.query('INSERT INTO trades(account_id,direction,amount,entry_price,exit_price,result,pnl,expiry) VALUES($1,$2,$3,$4,$5,$6,$7,$8) RETURNING *',[accountId,direction,amt,entry,exit,result,pnl,expiry]);await pool.query('UPDATE accounts SET balance=balance+$1,equity=equity+$1,total_pnl=total_pnl+$1,daily_pnl=daily_pnl+$1 WHERE id=$2',[pnl,accountId]);return res.json({trade:nr.rows[0]})}const d=dbRead();const t={id:d.next.trade++,account_id:a.id,direction,amount:amt,entry_price:entry,exit_price:exit,result,pnl,expiry,created_at};d.trades.push(t);const ac=d.accounts.find(x=>x.id===a.id);ac.balance=Number(ac.balance)+pnl;ac.equity=Number(ac.equity)+pnl;ac.total_pnl=Number(ac.total_pnl)+pnl;ac.daily_pnl=Number(ac.daily_pnl)+pnl;dbWrite(d);res.json({trade:t})}catch(e){res.status(500).json({error:'Trade failed'})}});
function admin(req,res,next){if(req.headers['x-admin-key']!==ADMIN_KEY)return res.status(403).json({error:'Admin access denied'});next()}
app.get('/api/admin/orders',admin,async(req,res)=>{if(usePg){const r=await pool.query('SELECT o.*,u.name,u.email FROM orders o JOIN users u ON u.id=o.user_id ORDER BY o.id DESC');return res.json({orders:r.rows})}const d=dbRead();res.json({orders:d.orders.sort((a,b)=>b.id-a.id).map(o=>({...o,name:d.users.find(u=>u.id===o.user_id)?.name||'',email:d.users.find(u=>u.id===o.user_id)?.email||''}))})});
app.post('/api/admin/orders/:id/approve',admin,async(req,res)=>{try{if(usePg){const c=await pool.connect();try{await c.query('BEGIN');const o=(await c.query('SELECT * FROM orders WHERE id=$1 FOR UPDATE',[req.params.id])).rows[0];if(!o)throw Error('Order not found');if(o.status==='approved'){await c.query('COMMIT');return res.json({ok:true,message:'Already approved'})}const no='FE-'+String(Date.now()).slice(-7)+String(Math.floor(Math.random()*90+10));const a=await c.query('INSERT INTO accounts(user_id,account_no,mode,size,balance,equity,total_pnl) VALUES($1,$2,$3,$4,$4,$4,0) RETURNING *',[o.user_id,no,o.mode,o.size]);await c.query("UPDATE orders SET status='approved',approved_at=now() WHERE id=$1",[o.id]);await c.query('COMMIT');return res.json({ok:true,account:a.rows[0]})}finally{c.release()}}const d=dbRead();const o=d.orders.find(x=>x.id===Number(req.params.id));if(!o)throw Error('Order not found');if(o.status==='approved')return res.json({ok:true,message:'Already approved'});const no='FE-'+String(Date.now()).slice(-7)+String(Math.floor(Math.random()*90+10));const a={id:d.next.account++,user_id:o.user_id,account_no:no,mode:o.mode,size:o.size,balance:o.size,equity:o.size,total_pnl:0,daily_pnl:0,status:'active',created_at:new Date().toISOString()};d.accounts.push(a);o.status='approved';o.approved_at=new Date().toISOString();dbWrite(d);res.json({ok:true,account:a})}catch(e){res.status(400).json({error:e.message})}});
app.post('/api/admin/orders/:id/reject',admin,async(req,res)=>{if(usePg)await pool.query("UPDATE orders SET status='rejected' WHERE id=$1",[req.params.id]);else{const d=dbRead();const o=d.orders.find(x=>x.id===Number(req.params.id));if(o)o.status='rejected';dbWrite(d)}res.json({ok:true})});
const port=process.env.PORT||10000;init().then(()=>app.listen(port,()=>console.log('FundedEdge API listening on '+port+' storage='+(usePg?'postgres':'fallback-file')))).catch(e=>{console.error(e);process.exit(1)});